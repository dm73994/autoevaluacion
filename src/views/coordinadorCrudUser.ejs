<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>crud users</title>
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
</head>
<body>

    <nav class="navbar navbar-dark bg-dark">
        <a href="/" class="navbar-brand">crud users</a>
    </nav>
    
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-5">
                <table class="table table-bordered table-hover">
                    <thead>
                        <th>identificacion</th>
                        <th>nombre</th>
                        <th>apellido</Address></th>
                        <th>genero</th>
                        <th>email</th>
                        <th>estudios</th>
                        <th>activo</th>
                        <th>acciones</th>
                    </thead>
                    <tbody>
                        <% if(data) { %>
                            <% for (var i = 0; i < data.length ; i++){ %>
                                <% if(data[i].activo !== 1) { continue; } %> 
                        
                                <tr>
                                    <td><%= data[i].user_identification %></td>
                                    <td><%= data[i].user_name %></td>
                                    <td><%= data[i].user_lastname %></td>
                                    <td><%= data[i].user_gender %></td>
                                    <td><%= data[i].user_email %></td>
                                    <td><%= data[i].user_studies %></td>
                                    <td><%= data[i].activo %></td>
                                    <td>
                                        <a class="btn btn-info" href="/coordinadorUpdateUserView/<%= data[i].user_id %>">Update</a>
                                    </td>
                                </tr>
                        
                            <% }%>   
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

    </div>

</body>
</html>