<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Realizar Autoevaluación</title>
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" integrity="sha384-******************************" crossorigin="anonymous">
    <style>
        .navbar {
            display: flex;
            justify-content: space-between;
            background-color: #1d1281;
        }
        
        .navbar-brand {
            flex-grow: 1;
        }
        
        .table-container {
            margin-left: -200px;
            margin-right: auto;
            max-width: 500px;
        }
        
        .table-container table {
            margin-bottom: 0;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
        }

        .navbar-brand {
            flex-grow: 1;
        }
        
        .table-container {
            margin-left: -200px;
            margin-right: auto;
            max-width: 500px;
        }
        
        .table-container table {
            margin-bottom: 0;
        }

        .custom-nav {
            display: flex;
            align-items: center;
        }
        .create-link {
            margin-right: 9000px; 
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            position: sticky;
            top: 0;
            left: 0;
            right: 0;
            z-index: 9999;
        }

        .navbar-brand {
            padding: 5;
        }
        
        .navbar {
            position: relative;
            z-index: 1;
        }

        details {
            position: relative;
            display: inline-block;
            z-index: 2;
        }

        details summary {
            cursor: pointer;
        }

        details ul {
            position: absolute;
            top: 100%;
            left: 0;
            display: none;
            margin: 0;
            padding: 0;
            list-style: none;
            background-color: #9b1a1a;
        }

        details ul li {
            width: 100%;
        }

        details ul li a {
            display: block;
            padding: 10px;
            color: #fff;
            text-decoration: none;
            width: 300px;
        }
        details summary {
            cursor: pointer;
            white-space: nowrap; 
        }
        details ul li a:hover {
            background-color: #555;
        }

        details[open] ul {
            display: block;
        }

        .fondo{
            background-image: url(../img/fondo2.jpg);
            background-size: cover;
            background-repeat: no-repeat;
            margin-top: -46px;
            height: 1200px;
        }
        .form-group p{
        color: black;
        font-size: 17px;
    }

    </style>
</head>
<body>

    <nav class="navbar navbar-dark  navbar-expand-lg">
        <div class="navbar-brand"> 
            <a>realizar autoevaluacion</a>
            <details>
                <summary>&#9776;</summary>
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="btn btn-info" href="/docenteBandejaEntrada">Bandeja de entrada</a></li>
                    <li class="nav-item"><a class="btn btn-info" href="/docenteAutoevaluaciones">Consultar autoevaluaciones</a></li>
                    <li class="nav-item"><a class="btn btn-info" href="/docenteInformacionPersonal">Consultar informacion personal</a></li>
                    <li class="nav-item"><a class="btn btn-info" href="/docenteDiligenciar">Realizar autoevaluación</a></li>
                </ul>
            </details>
        </div>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item"> 
                <a class="navbar-brand home-link" href="/docentePrincipal"> 
                    <i class="fas fa-home"></i>
                </a>
            </li>
        </ul>
    </nav>

    <div class="fondo">
        <div class="container mt-5">
            <div class="row">
                <div class="col-md-7">
                    <div class="card">
                        <div class="card-body">
                            <form action="/docenteDiligenciarAutoevaluacion/<%= data.autoevaluation_id %>" method="post">
                                <table class="table">
                                    <tbody>
                                        <tr>
                                            <th>Identificación de Usuario</th>
                                            <td><%= data.user_identification %></td>
                                        </tr>
                                        <tr>
                                            <th>Código de Labor</th>
                                            <td><%= data.labor_code %></td>
                                        </tr>
                                        <tr>
                                            <th>ID del Período</th>
                                            <td><%= data.period_id %></td>
                                        </tr>
                                        <tr>
                                            <th>Acto</th>
                                            <td>
                                                <% if (data.acto === 'si') { %>
                                                    Si
                                                <% } else { %>
                                                    No
                                                <% } %>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Estado</th>
                                            <td><%= data.state %></td>
                                        </tr>
                                        <tr>
                                            <th>Fecha de Inicio</th>
                                            <td><%= data && data.date_init ? data.date_init.toISOString().substring(0, 10) : '' %></td>
                                        </tr>
                                        <tr>
                                            <th>Fecha de Finalización</th>
                                            <td><%= data && data.date_finish ? data.date_finish.toISOString().substring(0, 10) : '' %></td>
                                        </tr>
                                        <tr>
                                            <th>Activo</th>
                                            <td>
                                                <% if (data.activo === 1) { %>
                                                    Activo
                                                <% } else { %>
                                                    Inactivo
                                                <% } %>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Resultados</th>
                                            <td>
                                                <textarea name="resultados" class="form-control" rows="8"><%= data.resultados %></textarea>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Evaluación</th>
                                            <td>
                                                <input type="number" name="evaluacion" value="<%= data.evaluacion %>" class="form-control" min="0" max="100">
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>sugerencias</th>
                                            <td>
                                                <textarea name="sugerencias" class="form-control" rows="8"><%= data.sugerencias %></textarea>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                                <button type="submit" class="btn btn-primary mx-auto d-block">
                                    Enviar
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if(typeof alert != "undefined") { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>' ,
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmationButton: <%= showConfirmButton %>,
                timer:<%= timer %>
            }).then(()=>{
                window.location='/<%= ruta %>'
            })
        </script>
    <% } %>
</body>
</html>